<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minimoneybox">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IndividualAccountLifetime"></activity>
        <activity
            android:name=".IndividualAccountStockShares"
            android:label="@string/title_activity_individual_account_stock_shares"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IndividualAccountGeneralInvestmentAccount"
            android:label="@string/title_activity_individual_account_general_investment_account"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".user"
            android:label="@string/title_activity_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
    </application>

</manifest>